describe('database-updates bin', () => {
  it('should error if no database provided')

  it('should exit with 0 if successfully run')

  describe('create', () => {
    it('should create a new update script')
  })
})
